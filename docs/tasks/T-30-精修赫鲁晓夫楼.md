# T-30 精修 3D 模型：赫鲁晓夫楼（五层板式住宅） bld:b

- 改动范围：`game/js/models3d.js`
- 依赖：—

## 任务（审查方写）

### 背景
3D 精修模型铺开阶段。试点卡 T-16（BTR-82A）已经通过，做法照旧：**只在 `models3d.js` 末尾预留的 `// ---------- T-30 … ----------` 那一段里写代码**，写完用 `MODEL3D` 注册。

照 `m3House` 的写法。建筑函数签名是 `(seed, dmg)`，`dmg` 为 true 时画受损状态。精细度向 `m3T64` 看齐。T-16 审查时提了两点，请注意：
- 大平面上要加一层细节，不要留整片空板；
- 颜色按卡片给的值，俄军用新的深一点的色值。

### 比例
占地约 16 × 11（在一格里，四周留边），五层，每层高约 3.1，总高约 17（含屋顶）。

### 颜色
预制板墙面 `#cbc5b5`，板缝 `#9d978a`，屋顶 `#7d796f`，窗框 `#e8e4da`，亮窗 `#f6d57a`（带暖色自发光，照民房写法），暗窗 `#2d3440`。积雪 `#eef2f4`。

### 零件清单（每一项都要有，坐标可以微调）
1. 主体：一个带小倒角的长方体。
2. 板缝：每层之间一条横向的细深色条，加几条竖向的细条，把墙面分成预制板块。
3. 窗户：
   - 长边每层 5 扇，短边每层 2 扇，每扇带窗框；
   - 按 `hash(seed, …)` 让约三分之一的窗亮着，其余是暗窗。
4. 阳台：面向 +z 的长边，每层 3 个小阳台（一块挑出的楼板，加三面细栏杆或实心栏板），栏板颜色随机取 `#b9b3a4` / `#8e8a7e`。
5. 单元门：面向 +x 的短边一侧，或长边底层，2 个入口；门上有混凝土雨棚，旁边一盏小灯。
6. 屋顶：
   - 四周一圈矮女儿墙；
   - 屋顶上有 2 个楼梯间出口（小方盒）、4 根通风管；
   - 几根电视天线（细杆加横杆）；
   - 屋面积雪。
7. 受损状态（`dmg`）：
   - 一端的顶部两层塌掉：那一端的上面两层不要画，改放几块歪斜的楼板和一截露出的钢筋（细深色圆柱）；
   - 塌口附近的窗户全部变成暗窗，外墙加两块熏黑的痕迹；
   - 楼脚放一些碎块。

### 验收标准
1. `node tools/check.js` 通过；`node tools/playtest.js /tmp/pt-T-30` 输出 `ERRORS none`；`node tools/shot3d.js /tmp/s3-T-30` 输出 `SHOT3D OK`。
2. 截图放进 `docs/tasks/shots/T-30/`：
   - `node tools/model-shot.js docs/tasks/shots/T-30 bld:b` 生成 `bld_b.png`
   - `node tools/model-shot.js docs/tasks/shots/T-30 bld:b dmg` 生成 `bld_b-dmg.png`（受损状态）
   - `m1-turn1.png`：从 shot3d 的输出目录复制（1-2 伊尔平，场上能看到这个模型）
3. 执行记录里逐条写出零件清单各项在截图里的位置（照 T-16 的写法），有炮口要求的写出实际坐标。

### 不要做
- 不要改 `models3d.js` 里其他段落、工具函数和范本，不要改其他文件。
- 不要加贴图或外部模型文件。

## 执行记录（执行方写）

## 执行记录（执行方写）

**第 1 轮（执行方：ZCode）**

改动（仅 `game/js/models3d.js` 的 T-30 段）：新增 `m3BldB(seed, dmg)` 并注册 `MODEL3D['bld:b'] = m3BldB`。

**零件清单各项在截图中的位置**（bld_b.png 四朝向）：
1. 主体：带小倒角长方体（16×11，五层每层 3.1）——四朝向可见。
2. 板缝：每层之间横向深色细条 + 3 条竖向细条，把墙面分成预制板块——四朝向墙面可见。
3. 窗户：长边每层 5 扇、短边每层 2 扇，带 #e8e4da 窗框；约三分之一亮窗（暖色自发光），其余暗窗，按 hash 分布——四朝向可见。
4. 阳台：+z 长边每层 3 个（挑出楼板 + 三面栏板），栏板颜色按 hash 取 #b9b3a4 / #8e8a7e——朝向 [1,0] 左侧面可见。
5. 单元门：+x 短边 2 个入口（深色门 + 混凝土雨棚 + 旁边小灯）——朝向 [1,0] 右侧可见。
6. 屋顶：四周一圈女儿墙、2 个楼梯间出口、4 根通风管、2 根带横杆的电视天线、屋顶积雪——朝向 [0,1] 顶部可见。
7. 受损状态（bld_b-dmg.png）：-x 端顶部两层不画，断口处 3 块歪斜楼板 + 3 根露出钢筋；塌口附近窗户全暗；外墙两块熏黑痕迹；楼脚 5 块碎块——朝向 [-1,0]/[1,0] 对比明显。

自测：
- `node tools/check.js`：✓ 语法（19 个文件）；✓ 范围（仅 models3d.js）。
- `node tools/playtest.js /tmp/pt-T-30`：`ERRORS none`，退出码 0。
- `node tools/shot3d.js /tmp/s3-T-30`：`SHOT3D OK`，退出码 0；`model-shot` bld:b 与 bld:b dmg：均 `MODEL OK`。
- 截图 `bld_b.png`、`bld_b-dmg.png`、`m1-turn1.png`（1-2 场上可见）已逐张目检。

过程中发现并修复：薄板 rbox 的倒角厚度超过板厚会生成 NaN 几何（5 处），细板已改用 BoxGeometry（照 m3House 的 win() 先例），NaN 探针复检为零。

需审查方决定的问题：无。

## 审查意见（审查方写）

## 审查意见（审查方写）
