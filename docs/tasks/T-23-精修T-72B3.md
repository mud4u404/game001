# T-23 精修 3D 模型：T-72B3 主战坦克 t72

- 改动范围：`game/js/models3d.js`
- 依赖：—

## 任务（审查方写）

### 背景
3D 精修模型铺开阶段。试点卡 T-16（BTR-82A）已经通过，做法照旧：**只在 `models3d.js` 末尾预留的 `// ---------- T-23 … ----------` 那一段里写代码**，写完用 `MODEL3D` 注册。

完全照 `m3T64` 的写法，但它必须和 T-64 一眼就能区分。精细度向 `m3T64` 看齐。T-16 审查时提了两点，请注意：
- 大平面上要加一层细节，不要留整片空板；
- 颜色按卡片给的值，俄军用新的深一点的色值。

### 比例
尺寸与 T-64 相同（车体长约 18，含炮管约 26）。

### 颜色
俄军涂装：主色 `#5a5f4b`，暗部 `#3f4234`，亮部 `#686d58`。不画任何字母标记（不要 Z、V）。 通用：橡胶 `#1e201d`，钢件 `#5d625c`（金属度 0.5），深色件 `#2d302a`，玻璃 `#223044`（粗糙度 0.1）。 反应装甲 `#646a53`。

### 零件清单（每一项都要有，坐标可以微调）
1. 行走装置：每侧 6 个**大**负重轮（半径约 1.4，比 T-64 的明显大一圈），3 个托带轮，后部主动轮，前部诱导轮，`trackLoop` 履带。这是它和 T-64 最明显的区别。
2. 侧裙板：前三段挂“接触-5”反应装甲块（大而平的方块），后段是普通橡胶裙板。
3. 首上装甲：一整片“接触-5”大平板（2 排，每排 4 块，平铺而不是 T-64 那种小砖块）。
4. 炮塔：更圆、更低的铸造炮塔；正面左右各一组呈 V 字形的楔形反应装甲（每组 3~4 块向外倾斜的厚板）。
5. 炮塔顶：
   - 车长指挥塔，上面装遥控 12.7 毫米机枪（长枪管加弹箱）；
   - 右前方一个方盒形的“松树-U”炮长瞄准镜，正面有两块玻璃。
6. 烟幕弹发射器：炮塔正面两侧，呈弧形排列，每侧 4 具。
7. 炮塔尾部：储物箱，以及一根横放的通气管（长圆柱）。
8. 车尾：两个油桶（照 T-64）。
9. 125 毫米炮：带热护套分段、抽烟装置、炮口。不要黄色识别胶带。
10. 炮塔放进 `turret` 组。炮口：`MUZZLE.t72` 是 `[13, 0, 6]`，和 T-64 一致。

### 验收标准
1. `node tools/check.js` 通过；`node tools/playtest.js /tmp/pt-T-23` 输出 `ERRORS none`；`node tools/shot3d.js /tmp/s3-T-23` 输出 `SHOT3D OK`。
2. 截图放进 `docs/tasks/shots/T-23/`：
   - `node tools/model-shot.js docs/tasks/shots/T-23 t72` 生成 `t72.png`
   - `m1-turn1.png`：从 shot3d 的输出目录复制（1-2 伊尔平，场上能看到这个模型）
3. 执行记录里逐条写出零件清单各项在截图里的位置（照 T-16 的写法），有炮口要求的写出实际坐标。

### 不要做
- 不要改 `models3d.js` 里其他段落、工具函数和范本，不要改其他文件。
- 不要加贴图或外部模型文件。

## 执行记录（执行方写）

## 执行记录（执行方写）

**第 1 轮（执行方：ZCode）**

改动（仅 `game/js/models3d.js` 的 T-23 段）：新增 `m3T72b3()` 并注册 `MODEL3D.t72 = m3T72b3`。俄军新色 `#5a5f4b/#3f4234/#686d58`、ERA `#646a53`。

**零件清单各项在截图中的位置**（t72.png 四朝向）：
1. 行走装置：每侧 6 个**大**负重轮（半径 1.4，明显比 T-64 大一圈）、3 个托带轮、**后部**带齿主动轮、**前部**诱导轮、trackLoop 履带——侧面四朝向可见，这是与 T-64 最明显的区别。
2. 侧裙板：前三段"接触-5"反应装甲大块 + 后段普通橡胶裙板——两侧可见。
3. 首上装甲：一整片 ERA 大板（2 排 × 4 块平铺大方块）——朝向 [1,0] 前部可见。
4. 炮塔：更圆更低的铸造圆顶；正面左右各一组 V 字楔形反应装甲（各 4 块外倾厚板）——朝向 [1,0] 正面可见。
5. 炮塔顶：车长指挥塔 + 遥控 12.7 毫米机枪（长枪管 + 弹箱）；右前方"松树-U"炮长瞄准镜（正面两块玻璃）——朝向 [0,1] 顶部可见。
6. 烟幕弹发射器：炮塔正面两侧弧形排列，每侧 4 具——朝向 [1,0] 正面可见。
7. 炮塔尾部：储物箱 + 横放通气管长圆柱——朝向 [-1,0] 可见。
8. 车尾：两个油桶（带端盖）——朝向 [-1,0]/[0,-1] 可见。
9. 125 毫米炮：热护套分段（5 道深色环）、抽烟装置、炮口——朝向 [1,0]/[-1,0] 可见；**无黄色识别胶带** ✓。

**炮口实际坐标**：与 T-64 相同，炮管口世界坐标约 (11.5, 5.3, 0)，与 MUZZLE.t72 [13, 0, 6]（换算 (11.5, 4.9) 附近）一致。

自测：
- `node tools/check.js`：✓ 语法（19 个文件）；✓ 范围（仅 models3d.js）。
- `node tools/playtest.js /tmp/pt-T-23`：`ERRORS none`，退出码 0。
- `node tools/shot3d.js /tmp/s3-T-23`：`SHOT3D OK`，退出码 0；`model-shot` t72：`MODEL OK`。
- 截图 `t72.png`、`m1-turn1.png`（1-2 场上可见敌 T-72）已逐张目检：大轮、深色、圆低炮塔与 T-64 一眼区分。

需审查方决定的问题：无。

## 审查意见（审查方写）

## 审查意见（审查方写）

### 第 1 轮
审查提交：115e271
结论：通过

- 和 T-64 一眼就能区分：颜色更灰，炮塔更圆更低，正面有 V 字楔形“接触-5”，首上装甲是大平板，炮塔顶有“松树-U”瞄准镜和遥控机枪，车尾有油桶。炮口坐标正确。
- 负重轮被侧裙板挡住了大半，“大负重轮”这个区别在游戏里不太看得出来。可以接受，不用改。
- **流程提醒**：本卡分支是从 `zcode/T-22` 开出来的，不是从集成分支开的，所以合并时把 T-22 第一版（还没通过审查的 BMP-2 和它的截图）也一起带进了集成分支。T-22 返工通过后会覆盖掉它，这次不用处理。以后每张新卡都必须按第 1 节的流程，从 `origin/claude/inspiring-johnson-7m5bvj` 开分支。
