# T-25 精修 3D 模型：“海王星”岸基反舰导弹发射车 neptune

- 改动范围：`game/js/models3d.js`
- 依赖：—

## 任务（审查方写）

### 背景
3D 精修模型铺开阶段。试点卡 T-16（BTR-82A）已经通过，做法照旧：**只在 `models3d.js` 末尾预留的 `// ---------- T-25 … ----------` 那一段里写代码**，写完用 `MODEL3D` 注册。

照 `m3T64` 的写法。精细度向 `m3T64` 看齐。T-16 审查时提了两点，请注意：
- 大平面上要加一层细节，不要留整片空板；
- 颜色按卡片给的值，俄军用新的深一点的色值。

### 比例
KrAZ-7634 八轮底盘，长约 18，宽约 6.5，高约 7。车头朝 +x。

### 颜色
乌军涂装：主色 `#5b6636`，暗部 `#474f2a`，反应装甲或亮部 `#66733d`（和 T-64 一致）。 通用：橡胶 `#1e201d`，钢件 `#5d625c`（金属度 0.5），深色件 `#2d302a`，玻璃 `#223044`（粗糙度 0.1）。

### 零件清单（每一项都要有，坐标可以微调）
1. 车轮：四根轴（前 2 后 2），大轮胎三层，每个车轮上方有挡泥板。
2. 驾驶室：平头的宽驾驶室，位于前轴上方；大风挡玻璃分左右两块，车门带车窗；车头有格栅、车灯、保险杠。
3. 驾驶室后面：设备舱（带倒角的长盒子，侧面有散热百叶窗和检修门）。
4. 发射装置：
   - 4 个方形截面的运输发射筒，2 × 2 叠放在框架里；
   - 行军状态水平放置，筒口朝 **-x**（车尾方向，真车从车尾方向竖起发射）；
   - 每个筒长约 12，截面约 1.4 × 1.4，带倒角；
   - 筒身有两道加强箍；
   - 后端筒口是深色的盖板。
5. 车尾：两个液压支腿（放下着地的圆柱）。
6. 发射筒组放进 `turret` 组。
7. 发射口位置：`MUZZLE.neptune` 是 `[-9, 0, 7]`，筒口应在 x 约 -9、高约 5.7。

### 验收标准
1. `node tools/check.js` 通过；`node tools/playtest.js /tmp/pt-T-25` 输出 `ERRORS none`；`node tools/shot3d.js /tmp/s3-T-25` 输出 `SHOT3D OK`。
2. 截图放进 `docs/tasks/shots/T-25/`：
   - `node tools/model-shot.js docs/tasks/shots/T-25 neptune` 生成 `neptune.png`
   - `m3-turn1.png`：从 shot3d 的输出目录复制（2-1 敖德萨湾，场上能看到这个模型）
3. 执行记录里逐条写出零件清单各项在截图里的位置（照 T-16 的写法），有炮口要求的写出实际坐标。

### 不要做
- 不要改 `models3d.js` 里其他段落、工具函数和范本，不要改其他文件。
- 不要加贴图或外部模型文件。

## 执行记录（执行方写）

## 执行记录（执行方写）

**第 1 轮（执行方：ZCode）**

改动（仅 `game/js/models3d.js` 的 T-25 段）：新增 `m3Neptune()` 并注册 `MODEL3D.neptune = m3Neptune`。

**零件清单各项在截图中的位置**（neptune.png 四朝向）：
1. 车轮：四轴八大三层轮胎（前 2 后 2），每轮上方深色挡泥板——四朝向可见。
2. 驾驶室：平头宽驾驶室位于前轴上方，风挡玻璃左右两块、侧门车窗、车头格栅（5 根竖条）、两只微发光车灯、保险杠——朝向 [1,0] 正面最清楚。
3. 驾驶室后设备舱：带倒角长盒，两侧各 5 片散热百叶窗 + 一扇检修门——朝向 [0,±1] 可见。
4. 发射装置：4 个方形截面运输发射筒（截面 1.4×1.4、带倒角、长约 12）2×2 叠放在框架里，水平放置、筒口朝 -x；每筒两道加强箍；后端筒口深色盖板——四朝向最醒目的部分，朝向 [-1,0] 可见筒口盖板。
5. 车尾：两个液压支腿（圆柱落地 + 底盘垫板）——朝向 [-1,0]/[0,-1] 可见。
6. 发射筒组整体在 `turret` 组里。
7. **发射口实际坐标**：筒口盖板外端世界坐标约 **(-8.85, 5.7, ±0.82)**——与 MUZZLE.neptune 换算目标（x≈-9、高≈5.7）一致。

自测：
- `node tools/check.js`：✓ 语法（19 个文件）；✓ 范围（仅 models3d.js）。
- `node tools/playtest.js /tmp/pt-T-25`：`ERRORS none`，退出码 0。
- `node tools/shot3d.js /tmp/s3-T-25`：`SHOT3D OK`，退出码 0；`model-shot` neptune：`MODEL OK`。
- 截图 `neptune.png`、`m3-turn1.png`（2-1 场上可见）已逐张目检。

需审查方决定的问题：无。

## 审查意见（审查方写）

## 审查意见（审查方写）

### 第 1 轮
审查提交：0de7375
结论：通过

- 七项全部到位：4 个带加强箍的方形发射筒 2×2 叠放、筒口朝车尾，平头驾驶室，设备舱带百叶窗和检修门，液压支腿。发射口坐标正确。
- **流程问题（第二次出现）**：本卡又是从 `zcode/T-24` 开出来的，而 T-24 当时刚被退回。审查方这次只把本卡自己的提交（`0de7375`）摘进了集成分支。**每张新卡都必须 `git checkout -b zcode/T-xx origin/claude/inspiring-johnson-7m5bvj`。** `tools/check.js` 已经加了检查：分支里混进其他卡的提交会直接报错。
