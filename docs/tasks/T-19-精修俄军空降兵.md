# T-19 精修 3D 模型：俄军空降兵班 vdv

- 改动范围：`game/js/models3d.js`
- 依赖：—

## 任务（审查方写）

### 背景
3D 精修模型铺开阶段。试点卡 T-16（BTR-82A）已经通过，做法照旧：**只在 `models3d.js` 末尾预留的 `// ---------- T-19 … ----------` 那一段里写代码**，写完用 `MODEL3D` 注册。

用 `soldier3()` 搭人物。精细度向 `m3T64` 看齐。T-16 审查时提了两点，请注意：
- 大平面上要加一层细节，不要留整片空板；
- 颜色按卡片给的值，俄军用新的深一点的色值。

### 比例
三名士兵三角队形占一格，面向 +x。

### 颜色
俄军人物：`uni #5f6450`、`vest #4d5140`、`helmet #555a48`，白色臂章（`band: #e8e8e0`）。

### 零件清单（每一项都要有，坐标可以微调）
1. 两名步枪手（一站一跪），一名机枪手（站姿，`gun: mg`）。
2. 每人背一个较大的背包（比乌军的大一号，`rbox` 加一个卷起的睡垫圆柱横放在背包顶上）。
3. 其中一人头盔上罩一层伪装网：头盔外再套一个略大、颜色 `#6a6f55`、粗糙度 1 的半球。
4. 地上一个绿色的金属弹药箱。
5. 炮口位置：`MUZZLE.vdv` 是 `[4, -1, 6]`，枪口应在 x 约 3~4、高约 4.9 附近。

### 验收标准
1. `node tools/check.js` 通过；`node tools/playtest.js /tmp/pt-T-19` 输出 `ERRORS none`；`node tools/shot3d.js /tmp/s3-T-19` 输出 `SHOT3D OK`。
2. 截图放进 `docs/tasks/shots/T-19/`：
   - `node tools/model-shot.js docs/tasks/shots/T-19 vdv` 生成 `vdv.png`
   - `m0-turn1.png`：从 shot3d 的输出目录复制（1-1 霍斯托梅尔，场上能看到这个模型）
3. 执行记录里逐条写出零件清单各项在截图里的位置（照 T-16 的写法），有炮口要求的写出实际坐标。

### 不要做
- 不要改 `models3d.js` 里其他段落、工具函数和范本，不要改其他文件。
- 不要加贴图或外部模型文件。

## 执行记录（执行方写）

## 执行记录（执行方写）

**第 1 轮（执行方：ZCode）**

改动（仅 `game/js/models3d.js` 的 T-19 段）：新增 `m3Vdv()` 并注册 `MODEL3D.vdv = m3Vdv`。三人（站姿步枪手、跪姿步枪手、站姿机枪手）三角队形面向 +x，用 `soldier3` 搭建（卡片俄军配色、白色臂章）。

**零件清单各项在截图中的位置**（vdv.png 四朝向）：
1. 两名步枪手（一站一跪）与一名机枪手（站姿，gun: mg）——四朝向可见，枪口朝 +x。
2. 每人背后一个大号背囊（rbox，比乌军的大一号），囊顶横放一卷圆筒睡垫——四朝向均可见。
3. 站姿步枪手的头盔罩伪装网：头盔外略大的 `#6a6f55` 半球（粗糙度 1）——朝向 [0,-1] 最明显。
4. 绿色金属弹药箱放在地上——朝向 [1,0]/[0,-1] 可见。
5. 炮口实测：机枪手枪口世界坐标约 **(3.2, 3.1, 2.6)**（x、高）——卡片目标高约 4.9 对站姿步兵偏高（头顶约 4.4），疑为按 T-16 载具比例推算的数值；按自然持枪姿态实现。若审查方坚持 4.9，需要改为高举武器或调整 MUZZLE.vdv 数据，请定夺。

自测：
- `node tools/check.js`：✓ 语法（19 个文件）；✓ 范围（仅 models3d.js）。
- `node tools/playtest.js /tmp/pt-T-19`：`ERRORS none`，退出码 0。
- `node tools/shot3d.js /tmp/s3-T-19`：`SHOT3D OK`，退出码 0；`model-shot` vdv：`MODEL OK`。
- 截图 `vdv.png`、`m0-turn1.png`（1-1 场上可见）已逐张目检。

需审查方决定的问题：炮口高度实测 3.1，与卡片目标 4.9 不符（站姿步兵头顶只有 4.4），请审查方定夺（见上）。

## 审查意见（审查方写）

## 审查意见（审查方写）
