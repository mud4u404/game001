# T-20 精修 3D 模型：撤离的平民 civ

- 改动范围：`game/js/models3d.js`
- 依赖：—

## 任务（审查方写）

### 背景
3D 精修模型铺开阶段。试点卡 T-16（BTR-82A）已经通过，做法照旧：**只在 `models3d.js` 末尾预留的 `// ---------- T-20 … ----------` 那一段里写代码**，写完用 `MODEL3D` 注册。

用 `soldier3()` 搭人物（`gun: none`、`band: null`），`vest` 当作外套颜色，`uni` 当作裤子颜色。精细度向 `m3T64` 看齐。T-16 审查时提了两点，请注意：
- 大平面上要加一层细节，不要留整片空板；
- 颜色按卡片给的值，俄军用新的深一点的色值。

### 比例
四个人占一格，面向 +x（行进方向）。孩子用 `soldier3` 建好后整体缩放 0.65。

### 颜色
冬季外套用低饱和的颜色：`#7a4b3a`、`#3d4f6b`、`#6b6a5e`、`#8a7a52`；裤子 `#2f3035` / `#3b3a36`。皮肤 `#e0b896` 或 `#d6ad86`。

### 零件清单（每一项都要有，坐标可以微调）
1. 老年妇女：戴头巾（`hat` 用 `#8a3a3a`，再在帽子下方加一段围住脖子的扁圆环），手里提一个布包。
2. 男人：手提一个行李箱（深棕色带倒角的盒子，加一个把手），另一只手搭在孩子肩上的位置。
3. 女人：背一个双肩包，牵着孩子。
4. 孩子：缩放 0.65，戴红色毛线帽，怀里抱一个小玩具（小熊：两个小球加两个耳朵）。
5. 人物不要拿武器，不要戴头盔，不要有臂章。

### 验收标准
1. `node tools/check.js` 通过；`node tools/playtest.js /tmp/pt-T-20` 输出 `ERRORS none`；`node tools/shot3d.js /tmp/s3-T-20` 输出 `SHOT3D OK`。
2. 截图放进 `docs/tasks/shots/T-20/`：
   - `node tools/model-shot.js docs/tasks/shots/T-20 civ` 生成 `civ.png`
   - `m1-turn1.png`：从 shot3d 的输出目录复制（1-2 伊尔平，场上能看到这个模型）
3. 执行记录里逐条写出零件清单各项在截图里的位置（照 T-16 的写法），有炮口要求的写出实际坐标。

### 不要做
- 不要改 `models3d.js` 里其他段落、工具函数和范本，不要改其他文件。
- 不要加贴图或外部模型文件。

## 执行记录（执行方写）

## 执行记录（执行方写）

**第 1 轮（执行方：ZCode）**

改动（仅 `game/js/models3d.js` 的 T-20 段）：新增 `m3Civ()` 并注册 `MODEL3D.civ = m3Civ`。四人用 `soldier3` 搭建（`gun: 'none'`、`band: null`——无武器无臂章；`vest` 当外套、`uni` 当裤子），男人与女人用发色 `hat` 避免 soldier3 的默认军盔。

**零件清单各项在截图中的位置**（civ.png 四朝向）：
1. 老年妇女（左后，x 0.5、z -2.8）：红头巾（hat #8a3a3a）+ 头巾下围脖的扁圆环；手里的布包在身侧 (+z)——朝向 [1,0] 最清楚。
2. 男人（右前，x 2.6）：深棕带倒角行李箱 + 把手在右手 (-z 侧)；另一只手伸向孩子肩部方向的斜臂——朝向 [1,0]/[0,1] 可见。
3. 女人（左前，x -1.1、z 2.7）：背上双肩包，一只手朝孩子方向伸出（牵手）——朝向 [0,1]/[-1,0] 可见。
4. 孩子（两人中间，x 1.3、z 2.3）：整体缩放 0.65，红色毛线帽，怀里小熊（身 + 头 + 双耳）——四朝向均可见。
5. 无武器、无头盔、无臂章 ✓；皮肤 #e0b896 / #d6ad86。

无炮口要求（平民无武器）。

自测：
- `node tools/check.js`：✓ 语法（19 个文件）；✓ 范围（仅 models3d.js）。
- `node tools/playtest.js /tmp/pt-T-20`：`ERRORS none`，退出码 0。
- `node tools/shot3d.js /tmp/s3-T-20`：`SHOT3D OK`，退出码 0；`model-shot` civ：`MODEL OK`。
- 截图 `civ.png`、`m1-turn1.png`（1-2 伊尔平，路上可见平民）已逐张目检。

需审查方决定的问题：无。

## 审查意见（审查方写）

## 审查意见（审查方写）

### 第 1 轮
审查提交：b547c6a
结论：通过

- 四个人物都齐：红头巾老奶奶提着布包，男人拎着行李箱，女人背着双肩包，孩子戴红帽、抱着小熊。没有武器、头盔和臂章，外套颜色克制。
- 人物动作偏僵硬，这是 `soldier3` 范本本身的问题，审查方之后会统一改进范本，本卡不用返工。
