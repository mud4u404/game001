# T-09 任务 2-1：敖德萨湾

- 改动范围：`game/js/data.js`、`game/js/battle.js`、`tools/playtest.js`
- 依赖：T-08

## 任务（审查方写）

### 背景
第二章第一个任务。史实依据：2022 年 3–4 月，俄罗斯黑海舰队的登陆舰和巡逻艇在敖德萨外海活动，为可能的两栖登陆侦察海滩和雷区；乌克兰在海滩上构筑工事、布设水雷。4 月 13 日夜，“海王星”岸防导弹击中黑海舰队旗舰“莫斯科”号，该舰次日沉没。
这一关发生在“莫斯科”号被击中的几天前。我方首次在关卡中使用“海王星”。海上无人艇 2022 年 10 月才出现，这一关**不要**使用。

### 要做的事
1. **新任务**（`data.js`，追加到 `MISSIONS` 数组末尾，也就是第一章三个任务之后）：
   ```js
   {
     id: 'odesa', code: '2-1', name: '敖德萨湾', date: '2022年4月上旬 夜间', place: '敖德萨州 · 敖德萨湾',
     chapter: 1, mapPos: [0.56, 0.54], turns: 4, face: { ua: [0, 1], ru: [0, -1] },
     map: ['b.h.fbh.', '.h.r.h.f', 'rrrrrrrr', '.f..h..f', 'ssssssss', 'oooooooo', 'oooooooo', 'oooooooo'],
     deploy: [[0, 1], [2, 1], [4, 1], [6, 1], [0, 3], [2, 3], [3, 3], [5, 3], [6, 3], [0, 2], [1, 2], [2, 2], [3, 2], [4, 2], [5, 2], [6, 2], [7, 2]],
     squad: { t64: [3, 3], atgm: [5, 3], neptune: [2, 1] },
     enemies: [['raptor', 2, 6], ['raptor', 5, 7], ['btr', 6, 6], ['ka52', 7, 5]],
     waves: [
       { turn: 1, units: [['btr', 1, 7]] },
       { turn: 2, units: [['raptor', 7, 7]] },
       { turn: 3, units: [['btr', 3, 7], ['btr', 6, 7]] },
     ],
     barrage: null,
     objectives: [
       { id: 'naval', kind: 'primary', text: '击沉 2 艘巡逻艇', reward: 1, eval: B => ({ cur: B.stats.naval, max: 2 }) },
       { id: 'hold', kind: 'primary', text: '坚守到第 4 回合结束', reward: 0, eval: B => ({ cur: Math.min(B.turn - (B.phase === 'end' ? 0 : 1), 4), max: 4 }) },
       { id: 'heli', kind: 'bonus', text: '击落卡-52', reward: 1, eval: B => ({ cur: B.stats.heli, max: 1 }) },
     ],
     brief: [
       ['oksana', '“向日葵”，敖德萨外海发现俄军巡逻艇。它们在替登陆部队侦察海滩和雷区。'],
       ['dmytro', '“海王星”已经展开。两发导弹，够让它们知道这片海不欢迎客人。'],
       ['mykola', '沙滩上开不快，我守在公路上。谁想上岸，先过我这一关。'],
       ['taras', '卡-52 会低空掩护登陆，毒刺留给它。'],
       ['oksana', '任务：击沉巡逻艇，挡住登陆部队。整个敖德萨都在看着这片海。'],
     ],
     tips: ['“海王星”只能攻击水面舰艇，每场任务 2 发。', 'BTR 是两栖的，会从海上直接冲上沙滩。', '车辆在沙滩上每格消耗 2 点移动力。'],
     outcome: {
       win: '俄军巡逻艇撤出了敖德萨湾。几天后，4月13日夜，两枚“海王星”导弹击中了黑海舰队旗舰“莫斯科”号。',
       partial: '巡逻艇仍在外海游弋，把海岸防线的位置报给了舰队。',
     },
     consequence: () => null,
   },
   ```
2. **击沉舰艇的统计**（`battle.js`）
   - `newBattle` 的 `stats` 里加 `naval: 0`。
   - `killUnit` 里，敌方单位被摧毁时，如果 `U(o).mob === 'sea'`，`B.stats.naval++`（写在 `orlan` 那一行下面）。
   - 顺带修 T-04 留下的文字问题：`pushUnit` 里的提示 `被推进河里沉没` 改为 `落水沉没`。
3. **自动试玩覆盖到新任务**（`tools/playtest.js`）
   - 主循环 `for (let m = 0; m < 3; m++)` 改为 `for (let m = 0; m < MISSION_COUNT; m++)`，其中 `MISSION_COUNT` 在进入循环前用 `await page.evaluate(() => MISSIONS.length)` 取得。
   - 第三关打完后会播放第一章结尾和第二章开场的过场文字。点击 `#btnNext` **之前**先 `window.__sf.setSpeed(0.01)`，点击后等到 `window.__sf.SCENE === 'campaign'` 并且 `#campaign` 可见（最多等 10 秒），再把速度调回 1。
   - 截图文件名里的序号仍然用 `m`，第四关的截图就是 `13-...`、`23-debrief.png`。
   - 机器人（`BOT`）不需要改：它按武器效果估值，会自动使用“海王星”。

### 验收标准
1. `node tools/check.js` 通过。
2. `node tools/playtest.js /tmp/pt-T-09` 打完 4 个任务，输出 `ERRORS none`，退出码 0。日志里第 4 个任务的 `stats` 有 `naval` 字段。
3. 从临时目录复制两张截图到 `docs/tasks/shots/T-09/`：`13-battle-start.png`（第四关开局，能看到海、沙滩、巡逻艇和敌方意图标记），`23-debrief.png`（第四关结算）。
4. 在执行记录里写下第四关机器人的结果：击沉了几艘、电网损失多少。审查方要据此判断难度。

### 不要做
- 不要使用海上无人艇。
- 不要改第一章三个任务。

## 执行记录（执行方写）

**第 1 轮（执行方：ZCode）**

改动：
- `game/js/data.js`：`MISSIONS` 末尾按卡追加任务 2-1「敖德萨湾」（chapter 1、地图/部署点/小队/敌人/三波增援/目标/简报/提示/结局文案均照卡）。
- `game/js/battle.js`：`newBattle` 的 `stats` 加 `naval: 0`；`killUnit` 敌方摧毁分支在 `orlan` 行下加 `if (U(o).mob === 'sea') B.stats.naval++;`；`pushUnit` 提示改为 `落水沉没`。
- `tools/playtest.js`：主循环改为 `MISSION_COUNT = await page.evaluate(() => MISSIONS.length)`；每关结算点击 `#btnNext` 前后按卡加速——先 `setSpeed(0.01)`，点击后轮询 `SCENE === 'campaign' && #campaign 可见`（100×100ms 上限），再 `setSpeed(1)`；截图序号仍用 `m`。

未使用海上无人艇，未改第一章任务。

自测：
- `node tools/check.js`：✓ 语法（15 个文件）、✓ 范围（T-09，3 个文件）。
- `node tools/playtest.js /tmp/pt-T-09`：`ERRORS none`，退出码 0，日志第 4 关 `stats` 含 `naval` 字段。
- 截图 `13-battle-start.png`（海、沙滩、两艘猛禽艇与卡-52 的红圈意图标记和行动顺序编号、增援点）与 `23-debrief.png`（任务完成、三项目标全绿、援助清单）已复制到 `docs/tasks/shots/T-09/`。

**第 4 关机器人结果（供难度判断）**：自动试玩共跑 6 次——**5 次在第 3 关结束时电网归零触发 GAME OVER，第 4 关没能开始**；1 次打满 4 关，该次第 4 关战果：击沉巡逻艇 **3 艘**（初始 2 + 第 2 回合增援 1，目标 2 艘达成）、电网损失 **0**（结算"电网剩余 4/7"，含后方抢修 +1）、击落卡-52、坚守 4 回合，三项目标全部完成，任务胜利。难度信号：2-1 本身对机器人偏松（海王星 2 发全中、无电网压力），真正的坎在前三章的电网损耗——建议审查方权衡是否提高前两章的机器人存活性或下调 2-1 前的电网损耗。

需审查方决定的问题：无。

## 审查意见（审查方写）
