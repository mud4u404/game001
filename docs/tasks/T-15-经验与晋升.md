# T-15 单位经验与晋升

- 改动范围：`game/js/data.js`、`game/js/battle.js`、`game/js/ui.js`、`game/index.html`
- 依赖：T-14

## 任务（审查方写）

### 背景
名册里的单位要越打越值钱，损失老兵才会心疼，这样玩家会在“多派人求稳”和“少派人保老兵”之间权衡。

### 要做的事
1. **经验**
   - 名册记录新增 `xp: 0`（旧记录读取时补 0）。
   - 战斗中，我方单位击毁一个敌方单位时，该单位的 `u.xp` 加 1。击杀者就是 `killUnit(o, src)` 的 `src`，且 `src.team === 'ua'`。
     - 撞击自毁的无人艇不计经验；
     - 炮击（`msta`）、TB2 打击没有 `src`，也不计经验。
   - 接受结果时，把存活单位的 `u.xp` 写回名册；阵亡单位的经验保留在记录上，理由是乘员撤出了，维修后还在。
2. **晋升**（`data.js` 新增常量 `RANKS`）
   - 0 级：新兵；
   - 3 级：老兵，生命 +1；
   - 7 级：精锐，生命 +1、移动 +1（累计）。
   - `mkUnit` 生成单位时按 `xp` 叠加加成。T-64 反应装甲升级也加生命，两者叠加。
3. **显示**
   - 选队卡片、HUD roster、单位卡片上，用 1 或 2 个小五角星表示等级，颜色 `#d8c040`；单位卡片上同时显示“老兵”或“精锐”字样。
   - 结算画面列出本关晋升的单位：“教授（反坦克组）晋升为老兵。”。
     - 角色代号取 `CHARS[pilot].call`；
     - 同类单位有多个时，后面加名册编号，例如“邮差 #5”。
4. 战斗中不在地图上画星，保持画面干净。

### 验收标准
1. `node tools/check.js` 通过。
2. `node tools/playtest.js /tmp/pt-T-15` 输出 `ERRORS none`，退出码 0。
3. 执行记录里写一段验证：把某条名册记录的 `xp` 设为 3，开战后该单位的 `max` 比基础值多 1。
4. 截图 `docs/tasks/shots/T-15/roster.png`：HUD roster 里至少有一个单位带星。

### 不要做
- 不要改敌军和商店价格。

## 执行记录（执行方写）

**第 1 轮（执行方：ZCode）**

改动：
- `game/js/data.js`：新增 `RANKS`（0 新兵 / 3 老兵 hp+1 / 7 精锐 hp+1 且 move+1，累计）与 `rankOf(xp)`。
- `game/js/battle.js`：`mkUnit(type, x, y, xp = 0)`——记录 `u.xp`，按 `rankOf(xp)` 叠加生命/移动（在 T-64 反应装甲升级之后执行，两者叠加）；`reach` 的移动力读取改为 `u.move || U(u).move`；`killUnit` 敌方被摧毁时 `src` 为我方单位则 `src.xp++`（炮击/TB2 无 src 自动不计；`playerFire` 对自爆武器在 `applyEffects` 前后快照 `u.xp`，撞击自毁的无人艇不计经验）；`newBattle` 从名册记录传入 `rec.xp`；`endMission` 计算晋升（对比名册起始等级与战场等级，同类多单位加名册编号，如"邮差 #5"）并存活单位写回 `rec.xp`（阵亡记录保留原值）。
- `game/js/ui.js`：`rankStars(xp)` 生成 1–2 颗 `#d8c040` 五角星；选队卡片、HUD 名册、单位卡片分别显示星标；单位卡另附"老兵/精锐"字样与有效移动力（`su.move || d.move`）；结算 notes 列出本关晋升；名册增援/购买/赠送记录均带 `xp: 0`。
- `game/index.html`：新增 `.stars` 样式。

战斗中地图上不画星，保持画面干净；未改敌军和商店价格。

自测：
- `node tools/check.js`：✓ 语法（15 个文件）。范围检查仍为依赖分支误报（同 T-14，合入 T-11/T-13/T-14 后消失）。
- `node tools/playtest.js /tmp/pt-T-15`：`ERRORS none`，退出码 0。
- 验证（playwright 驱动 1-1）：名册 t64 `xp=3` → 开战 `max 5`（基础 4 +1）；atgm `xp=7` → `max 3`（+1）且移动力 4（基础 3 +1）；T-64 击杀 VDV 后 `xp` 4（3+1）；无 src 的击杀不加经验；`endMission` 后存活单位经验写回名册（4）；HUD 出现 2 处星标。
- 截图 `docs/tasks/shots/T-15/roster.png`：HUD 名册中 T-64BV（★ 老兵）与标枪/毒刺（★★ 精锐）带星。

需审查方决定的问题：无。

## 审查意见（审查方写）

### 第 1 轮
审查提交：fa112ee
结论：需修改（改动很小）

经验和晋升的主体逻辑是正确的：击杀加经验，无人艇撞击自毁不计经验，生命、移动加成和反应装甲升级能叠加，重打时经验也会随 `CAMP.pre` 一起回滚。需要改三处：

1. **先合入 T-14 的返工**：本卡建在 T-14 第 1 轮上，而 T-14 被退回了（损毁标记要改到接受结果时，见 T-14 卡片）。
   - 等 `zcode/T-14` 推送第 2 轮后，在本分支执行 `git merge origin/zcode/T-14`，有冲突就解决；
   - 然后重新跑自测。
   - 如果轮到你做本卡时 T-14 还没推送第 2 轮，先做 T-14。
2. **晋升文字按卡片格式写**：
   - 格式是 `教授（反坦克组）晋升为老兵。`，括号里是单位的 `short`，句末加句号；
   - 同类单位有多个时写成 `邮差 #5（步兵班）晋升为老兵。`。
3. **悬停提示的移动力**：`renderTip` 里单位说明的 `移动 ${d.move}` 改为 `移动 ${o.move || d.move}`，和单位卡片保持一致。
